/* Injected as /config.js — copy and edit per profile (ADR 0016). */
window.__TEXLOOPER__ = {
  profile: "inhouse",
  apiBaseUrl: "http://127.0.0.1:8787",
  apiKey: "dev-change-me",
  ephemeral: false,
};
