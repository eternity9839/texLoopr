{
  "name": "texloopr",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:share": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "android:init": "nix develop .#android -c bash -c 'npm run tauri android init'",
    "android:debug": "nix develop .#android -c bash -c 'npm run tauri android build -- --debug --apk --target aarch64'",
    "android:release": "nix develop .#android -c bash -c 'npm run tauri android build -- --apk --target aarch64'",
    "android:debug:universal": "nix develop .#android -c bash -c 'npm run tauri android build -- --debug --apk'"
  },
  "dependencies": {
    "@fontsource/sora": "^5.2.5",
    "@fontsource/source-serif-4": "^5.2.5",
    "@preact/signals": "^2.0.4",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-shell": "^2",
    "preact": "^10.16.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.5.0",
    "@tauri-apps/cli": "^2",
    "@testing-library/preact": "^3.2.4",
    "jsdom": "^30.0.1",
    "typescript": "^5.2.2",
    "vite": "^5.4.11",
    "vitest": "^3.0.5"
  }
}
